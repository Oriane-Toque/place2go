{% for friend in friends %}
<div class="chip">
    <img src="{{ friend.avatar ?? app.friend.avatar }}" alt="Friend"> {{ friend|capitalize }}
    <i class="close fas fa-times"></i>
</div>
{% else %}
<p>Aucun ami pour le moment</p>
{% endfor %}