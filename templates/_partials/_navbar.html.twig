<nav id="nav" class="navbar navbar-expand-lg">
	<div class="container">

		<a class="navbar-brand" href="/">
			<img src="{{ asset('img/logo-place2go.svg') }}" class="logo-sticky" width="150" alt="Place 2 Go">
		</a>

		<button type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler">
			<i class="fas fa-bars"></i>
		</button>

		<div id="navbarSupportedContent" class="collapse navbar-collapse">

			<ul class="navbar-nav m-auto">
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_category_list') }}">Catégories</a>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuEvent" data-mdb-toggle="dropdown" aria-expanded="false">Sorties</a>
					<ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuEvent">
						<li>
							<a class="dropdown-item" href="{{ path('app_event_list') }}">Voir toutes les sorties</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_event_create') }}">Créer une sortie</a>
						</li>
					</ul>
				</li>
			</ul>

			{% if is_granted('ROLE_USER') %}
			<!-- Authenticated User Menu -->
				<div class="nav-item dropdown bt-login">
					<a href="#" class="nav-link dropdown-toggle py-1 d-flex align-items-center" id="connectedDropdown" data-mdb-toggle="dropdown" aria-expanded="false">
						<span class="me-2 d-none d-lg-inline small">{{ app.user.nickname }}</span>
						<img src="{{ app.user.avatar }}" class="img-profile rounded-circle" alt="avatar" height="35px" width="35px">
					</a>
					<div class="dropdown-menu" aria-labelledby="connectedDropdown">
						<a class="dropdown-item icon" href="{{ path('app_profile_profile') }}">
							<i class="fa fa-dashboard"></i>
							Mon espace perso</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item icon" href="{{ path('app_logout') }}">
							<i class="fa fa-power-off"></i>
							Déconnexion</a>
					</div>
				</div>

			{% else %}
				<!-- Register Dropdown Menu -->
				<div class="navbar-nav align-items-lg-center nav-right">
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
					</li>

					<li class="nav-item dropdown">
						<button class="btn btn-primary mb-3 mb-lg-0" id="loginDropdown" data-mdb-toggle="dropdown" aria-expanded="false" tabindex="0">Connexion</button>
						
						<div class="dropdown-menu login-form px-3 py-4" aria-labelledby="loginDropdown">
							{% include "_partials/_dropdown_login.html.twig" %}
						</div>
					</li>
				</div>
			{% endif %}

		</div>
	</div>
</nav>
