{{ form_start(form)}}

	<div class="py-3">

		<div class="row align-items-center mb-3">
			<div class="col-sm-12 col-md-9 mb-3">
				<div class="">
				{{ form_widget(form.q, { attr: {'class': 'form-control-lg'} }) }}
				</div>
			</div>
			<div class="col-sm col-md-3 mb-3">
				<button type="submit" class="btn btn-outline-info btn-lg btn-block" type="button"><i class="fas fa-search me-2"></i>Rechercher</button>
			</div>
			{# <div class="input-group mb-3">
				{{ form_widget(form.q, { attr: {'class': 'form-control-lg', 'aria-describedby': 'button-addon2'} }) }}
				<button class="btn btn-outline-info ms-3" type="submit" id="button-addon2" data-mdb-ripple-color="dark">
					Rechercher
				</button>
			</div> #}
		</div>

		{% if app.request.get('_route') == 'app_home' %}
			{{ form_widget(form.categories, {'attr': {'style': 'visibility:hidden'}})}}
			{{ form_errors(form.categories) }}
		{% else %}
			{{ form_widget(form.categories)}}
			{{ form_errors(form.categories) }}
		{% endif %}

	</div>

{{ form_end(form)}}
